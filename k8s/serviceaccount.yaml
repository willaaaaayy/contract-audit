# Выделенный ServiceAccount без токена Kubernetes API: приложение к API кластера
# не обращается, поэтому automount отключён (уменьшение поверхности атаки).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: contract-audit
  labels:
    app: contract-audit
automountServiceAccountToken: false
